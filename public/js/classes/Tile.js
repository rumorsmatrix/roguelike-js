class Tile {

    constructor(glyph, fg_color, bg_color, passable, light_passes)
    {
        this.glyph = glyph;
        this.fg_color = fg_color;
        this.bg_color = bg_color;

        this.passable = passable;
        this.light_passes = light_passes;
    }

}
